package com.moonevue.core.enums;

public enum TransactionStatus {
    DRAFT,
    CHECKOUT_OPEN,
    PROCESSING,
    PAID,
    EXPIRED,
    PENDING,
    AUTHORIZED,
    CAPTURED,
    SETTLED,
    CANCELED,
    FAILED,
    REFUNDED
}
